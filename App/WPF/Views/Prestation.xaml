<UserControl x:Class="WPF.Views.Prestation"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:WPF.Views"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#5271ff">
        <StackPanel Margin="0,10,0,0">
            <Grid Background="#77B5FE" Height="227" Width="770">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Label Content="Titre" Grid.Row="0" Grid.Column="0" Margin="10,28,10,68" Grid.RowSpan="2"/>
                <TextBox x:Name="titre_pres" Grid.Row="0" Width="150" Margin="83,28,34,68" Grid.RowSpan="2" Text="{Binding Titre, UpdateSourceTrigger=PropertyChanged}" Cursor="IBeam"/>

                <Label Content="Durée" Grid.Column="1" Margin="10,30,10,68" Grid.RowSpan="3"/>
                <TextBox x:Name="duree_pres" Grid.Column="1" Width="150" Margin="80,30,36,72" Grid.RowSpan="2" Text="{Binding Duree, UpdateSourceTrigger=PropertyChanged}"  Cursor="IBeam"/>

                <Label Content="Tarif" Grid.Row="1" Grid.Column="0" Margin="10,28,10,-8" Grid.RowSpan="2"/>
                <TextBox x:Name="tarif_pres" Width="150" Margin="83,30,34,24" Grid.RowSpan="1" Grid.Row="1" Text="{Binding Tarif, UpdateSourceTrigger=PropertyChanged}" Cursor="IBeam"/>

                <Label Content="Description" Grid.Row="0" Grid.Column="2" Margin="10"/>
                <TextBox x:Name="descip_pres" Grid.Row="1" Grid.Column="2" Grid.RowSpan="2" Margin="10"
                         Width="200" Height="120" TextWrapping="Wrap" AcceptsReturn="True" Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" Cursor="IBeam"/>

                <StackPanel Grid.Row="2" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Left" Margin="147,23,0,-3">
                    <Button Content="Ajouter" Width="100" Margin="10" Cursor="Hand" RenderTransformOrigin="0.5,0.5" Command="{Binding AjouterPrestation}"/>
                    <Button Content="Supprimer" Width="100" Margin="10" Cursor="Hand" Command="{Binding SupprimerPrestation}"/>
                    <Button Content="Modifier" Width="100" Margin="10" Cursor="Hand" Command="{Binding ModifierPrestation}"/>
                </StackPanel>
            </Grid>
            <DataGrid x:Name="dataGridView1" Height="201" ItemsSource="{Binding Prestations}" SelectedItem="{Binding Select, Mode=TwoWay}" AutoGenerateColumns="False" Width="772">
                <DataGrid.Columns>
                <DataGridTextColumn Header="Titre" Binding="{Binding Titre}" />
                <DataGridTextColumn Header="Tarif (€)" Binding="{Binding Tarif}" />
                <DataGridTextColumn Header="Durée" Binding="{Binding Duree}" />
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" />
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Grid>
</UserControl>
